{
  "name": "puttline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Golf green reading training tool",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
